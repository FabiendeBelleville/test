package bliblou;

import java.io.IOException;
import java.io.PrintWriter;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 * Servlet implementation class CestQui
 */
@WebServlet("/CestQui")
public class CestQui extends HttpServlet {
	private static final long serialVersionUID = 1L;

	/**
	 * @see HttpServlet#HttpServlet()
	 */
	public CestQui() {
		super();
	}

	/**
	 * @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse
	 *      response)
	 */
	protected void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		PrintWriter writer = response.getWriter();

		writer.println("<p>********************** Réponse du formulaire **********************");
		writer.println("<p> Nom : " + request.getParameter("nom"));
		writer.println("<p> password : " + request.getParameter("password"));
		writer.println("<p> sex : " + request.getParameter("sex"));

		String[] values = request.getParameterValues("pays");
		for (int i = 0; i < values.length; i++) {
			writer.println("<li>" + values[i] + "</li>");
		}

		writer.println(request.getParameterValues("cinema"));
		writer.println(request.getParameterValues("checkbox"));
		writer.println(request.getParameterValues("cinema"));
		writer.println("<p> hobbies : " + request.getParameterValues("hobbies"));
		
		
		
		
		
		
		String[] hobbies = request.getParameterValues("gastronomie");
		for (int i = 0; i < hobbies.length; i++) {
			writer.println("<li>" + hobbies[i] + "</li>");
		}
		
		
		
		
		
		
		
		
		
		writer.println("<p>********************************************");
		writer.println("<p>********************************************");
		response.getWriter().append("<p> Served at: ").append(request.getContextPath());
	}

	/**
	 * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse
	 *      response)
	 */
	protected void doPost(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		// TODO Auto-generated method stub
		doGet(request, response);
	}

}
